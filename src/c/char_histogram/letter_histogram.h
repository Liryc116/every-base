#ifndef LETTER_HISTOGRAM_H
#define LETTER_HISTOGRAM_H

// We assume h not necessarly cleared
// We consider only letters, Upper or Lower, without accents
void letter_histogram(char* str, char h[26]);

#endif
