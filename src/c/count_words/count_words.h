#ifndef COUNT_WORDS_H
#define COUNT_WORDS_H

// We count digits and letters (upper or lower, without accents) as in words
// Everything else will be considered a separator
size_t count_words(char* str);

#endif
