#ifndef BINSEARCH_H
#define BINSEARCH_H

#include <stdlib.h>

// Returns 1 if present, 0 else
// Easy to modify to return index or supposed index
int binsearch(int* array, size_t length, int x);

#endif

